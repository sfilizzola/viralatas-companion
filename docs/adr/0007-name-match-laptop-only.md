# Name match is laptop-only; remote lineup stays slot-based

**Name match** (announced name → official slot, same **Band**) is laptop `seed:bands:sync`. Godlike **Remote lineup sync** on the phone keeps matching Wacken by `slot_id` from **Official running order**. Mixing name-match into mid-festival slot moves would attach the wrong Band.

**Rejected:** Name-match inside remote lineup; drop remote lineup.
