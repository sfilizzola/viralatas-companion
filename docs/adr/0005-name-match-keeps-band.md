# Name match keeps the same Band

When laptop sync pairs one announced **Band** to one official slot by normalized name, it UPDATEs that row. Identity is the act, so picks and counts stay. Inserting a new slotted row and dropping the announced one would break **Band** identity.

**Rejected:** New Band per slot; exact-string-only match without normalize.
